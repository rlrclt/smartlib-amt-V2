# Agent Notes (Read First)

Repo root (WSL): `/mnt/c/smartlib-amt`

Goal: Firebase Hosting for frontend, Google Apps Script for lightweight "database".

Before doing anything:
- Read `AI_INDEX.md` for current state, commands, and file map.
- Do not re-run `firebase init` or re-scaffold the frontend unless explicitly requested (already initialized).

