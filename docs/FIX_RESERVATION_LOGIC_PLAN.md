# 🛠️ แผนการแก้ไข: ปัญหาจองหนังสือไม่ได้ (Logic Conflict)

วันที่: 2026-04-30
สถานะ: Ready for Implementation

## 1. คำอธิบายปัญหา
พบปัญหาความผิดพลาดเมื่อสมาชิกพยายามจองหนังสือที่ "ว่าง" อยู่ แต่ระบบแจ้งว่า `คุณกำลังยืมหนังสือเล่มนี้อยู่แล้ว` ซึ่งเกิดจาก Logic การตรวจสอบสถานะการยืม (Borrowing) ใน `Module_Loans.gs` มีความไม่รัดกุมในการเช็คสถานะ ทำให้สมาชิกที่ไม่สามารถจองหนังสือได้หากเคยมีประวัติการยืมในสถานะที่ระบบมองว่า "ยังไม่คืน"

## 2. สาเหตุเชิงเทคนิค
Logic ใน `Module_Loans.gs` (ฟังก์ชัน `reservationsCreate_`) ตรวจสอบสถานะการยืมกว้างเกินไป:
- ปัจจุบันเช็คเพียง `row.status !== "borrowing"` แต่ข้อมูลใน Sheet อาจมีสถานะอื่นที่คล้ายกันหรือข้อมูลค้าง
- ไม่ได้ตรวจสอบความสัมพันธ์ของข้อมูลย้อนหลังที่เพียงพอ

## 3. แผนการแก้ไข (Remediation Plan)

### งานส่วนที่ 1: ปรับปรุง Backend Logic (Strict Status Check)
แก้ไขการตรวจสอบสถานะการยืมให้แม่นยำขึ้น โดยเน้นแค่ `borrowing` และ `overdue`

**ไฟล์**: `apps_script/Module_Loans.gs`
```javascript
// ปรับปรุงบรรทัด 386 ใน reservationsCreate_
const hasBorrowing = readLoansRows_().some(function (row) {
  if (String(row.uid || "") !== actorUid) return false;
  
  // เพิ่มการเช็คสถานะที่ถูกต้องเท่านั้น
  const st = String(row.status || "").toLowerCase();
  if (st !== "borrowing" && st !== "overdue") return false; 
  
  const loanBookId = resolveBookIdByBarcode_(String(row.barcode || ""));
  return String(loanBookId || "") === bookId;
});
if (hasBorrowing) throw new Error("คุณมีรายการหนังสือเล่มนี้ที่ยังยืมอยู่ ไม่สามารถจองเพิ่มได้");
```

### งานส่วนที่ 2: เพิ่มสคริปต์ตรวจสอบข้อมูลย้อนหลัง (Data Audit Tool)
สร้างฟังก์ชันเพื่อตรวจสอบว่าสมาชิกรายนั้นๆ มี "Loan ค้าง" อยู่ในระบบจริงหรือไม่ก่อนจะทำการจอง

**ไฟล์**: `apps_script/Module_Loans.gs`
```javascript
function auditMemberLoans(uid) {
  const loans = readLoansRows_().filter(row => String(row.uid) === String(uid));
  return loans.map(row => ({
    barcode: row.barcode,
    status: row.status,
    dueDate: row.dueDate
  }));
}
```

## 4. ขั้นตอนการดำเนินการ (Execution Steps)
1. **Apply Backend Fix**: ปรับ Logic การเช็คสถานะใน `reservationsCreate_` ให้เป็นสถานะที่ชัดเจน
2. **Audit Data**: ใช้ `auditMemberLoans(uid)` ตรวจสอบสมาชิกที่มีปัญหากับข้อมูลยืมจริงใน Sheet ว่าสถานะค้างอยู่ที่ใด
3. **Data Repair**: หากพบ Loan ที่สถานะค้างทั้งที่คืนแล้ว ให้ทำการแก้ไขสถานะใน Google Sheets โดยตรง

---
*Generated by Gemini CLI*
