# 🛠️ แผนการแก้ไขปัญหา: ค่า "Uncategorized" ในรายงานสถิติ

วันที่: 2026-04-30
สถานะ: ออกแบบแผนงานเสร็จสิ้น (Ready for Implementation)

## 1. คำอธิบายปัญหา
พบค่า `Uncategorized` ในรายงานสถิติห้องสมุด เนื่องจาก:
1. **Frontend**: ช่อง `category` ในหน้า `/manage/register_books` ไม่ถูกบังคับกรอก (Optional) ทำให้ส่งค่าว่างเข้าสู่ระบบ
2. **Backend**: โค้ดรายงานใน `Module_ManageDashboard.gs` จะตั้งค่ากลุ่มข้อมูลเป็น `Uncategorized` หากไม่พบหมวดหมู่ที่ระบุ
3. **Data Discrepancy**: อาจมีข้อมูลเก่าที่ตกค้างจากการไม่มีหมวดหมู่ระบุ

## 2. แผนการแก้ไข (Remediation Plan)

### งานส่วนที่ 1: ป้องกันที่ต้นทาง (Frontend Validation)
บังคับให้ Admin ระบุหมวดหมู่ทุกครั้งที่ลงทะเบียนหนังสือใหม่
**ไฟล์**: `public/app/views/manage/register_books.view.js`
```javascript
// ปรับปรุงช่องหมวดหมู่ให้บังคับกรอก
<input name="category" required class="..." placeholder="เช่น ทั่วไป, เทคโนโลยี">
```

### งานส่วนที่ 2: ป้องกันที่ Backend (Data Sanitization)
บังคับให้ค่าว่างกลายเป็น "ทั่วไป" ทันทีที่ข้อมูลเข้ามา (Server-side Sanitization)
**ไฟล์**: `apps_script/Module_Books.gs`
```javascript
function normalizeBookCatalogInput_(payload) {
  // ...
  const category = String(payload && payload.category || "ทั่วไป").trim();
  return {
    category: category === "" ? "ทั่วไป" : category, // บังคับเป็น "ทั่วไป" หากส่งค่าว่าง
    // ...
  };
}
```

### งานส่วนที่ 3: ปรับ Logic รายงาน
แก้ไขการแสดงผลในรายงานให้统一กัน
**ไฟล์**: `apps_script/Module_ManageDashboard.gs`
```javascript
// เปลี่ยนจาก Uncategorized เป็น ทั่วไป
const category = catalogCategoryByBookId[bookId] || "ทั่วไป"; 
```

### งานส่วนที่ 4: ตรวจสอบข้อมูลเก่า (Audit)
สร้างฟังก์ชัน `auditOrphanedItems()` ใน `Module_BooksDataRepair.gs` เพื่อระบุรายการที่ไม่มีหมวดหมู่และจัดการแก้ไขย้อนหลังได้

## 3. ขั้นตอนการดำเนินการ (Execution)
1. **Frontend Fix**: บังคับ `required` ในหน้า `register_books.view.js`
2. **Backend Patch**: ปรับ Logic ใน `Module_Books.gs` และ `Module_ManageDashboard.gs`
3. **Data Audit**: รัน `auditOrphanedItems()` เพื่อตรวจดูว่ามีข้อมูลเก่าที่ค้างอยู่เท่าไหร่
4. **Data Repair**: รันสคริปต์แก้ไขข้อมูลเก่า (ถ้ามี) ให้เป็น "ทั่วไป" ทั้งหมด


---
*Generated by Gemini CLI*
