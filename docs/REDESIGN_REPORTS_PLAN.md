# 📱 แผนการ Redesign: ระบบรายงาน (Manage Reports) สไตล์ Native App

วันที่: 2026-04-30
สถานะ: Ready for Implementation

## 1. เป้าหมาย
ปรับปรุงหน้า `/manage/reports` จากเดิมที่เป็นรายการธรรมดา ให้เป็นหน้าจอที่มี UX/UI ระดับ Native App ตามเอกสาร `Library Reports Detailed.md`

## 2. การออกแบบหลัก (Core Design Principles)
ยึดตาม `docs/theme-Design/Library Reports Detailed.md` โดยมีคลาสหลักดังนี้:

### 2.1 CSS Base (ต้องเพิ่มลงใน `app.css`)
```css
.app-container {
  max-width: 480px; margin: 0 auto; background-color: #000;
  height: 100dvh; position: relative; overflow: hidden;
}

.view-pane {
  position: absolute; top: 0; left: 0; right: 0; bottom: 0;
  background-color: #F8FAFC; display: flex; flex-direction: column;
  transition: transform 0.4s cubic-bezier(0.32, 0.72, 0, 1), opacity 0.4s ease;
}

.pressable { transition: transform 0.15s cubic-bezier(0.32, 0.72, 0, 1); cursor: pointer; }
.pressable:active { transform: scale(0.96); opacity: 0.8; }

.skeleton-box {
  background: linear-gradient(90deg, #e2e8f0 25%, #f1f5f9 50%, #e2e8f0 75%);
  background-size: 200% 100%; animation: shimmer 1.5s infinite; border-radius: 8px;
}
@keyframes shimmer { 0% { background-position: -200% 0; } 100% { background-position: 200% 0; } }
```

### 2.2 โครงสร้างเทคนิค (Tech Stack)
- **Navigation**: ระบบ View Pane (Main/Sub) เลื่อนสไลด์ซ้าย-ขวา
- **Animations**: ใช้ Tailwind + Keyframe CSS (Fade-in, Shimmer, Bar-grow)
- **Interactions**: เพิ่ม Class `.pressable` สำหรับสร้างความรู้สึกการกดแบบ Native


## 3. รายละเอียดโค้ดตัวอย่าง

### 3.1 การจัดโครงสร้างหน้าจอ (Native Shell Pattern)
สร้าง Container ที่รองรับการสไลด์หน้าจอใน `public/app/views/manage/reports.view.js`:
```javascript
export function renderReportsView() {
  return `
    <div class="app-container relative h-full w-full bg-slate-50">
      <!-- Main Pane: รายการรายงาน -->
      <div id="main-view" class="view-pane view-main p-4">
        <h1 class="text-2xl font-black text-slate-800">รายงานห้องสมุด</h1>
        <div id="report-list" class="mt-4 grid gap-3">
          <button onclick="router.navigate('/manage/reports/R1')" class="pressable flex items-center justify-between rounded-2xl bg-white p-4 shadow-card">
            <span class="font-bold">รายงานสมาชิกเข้าใช้ (R1)</span>
            <i data-lucide="chevron-right" class="text-slate-400"></i>
          </button>
          <!-- รายการอื่นๆ... -->
        </div>
      </div>
      <!-- Sub Pane: สำหรับแสดงรายละเอียดรายงาน (จะถูกเปิดเมื่อกดเลือก) -->
      <div id="sub-view" class="view-pane view-sub"></div>
    </div>
  `;
}
```

### 3.2 การแสดงผลข้อมูลสถิติ (Native Chart Style)
ใช้ CSS Native Charts เพื่อความเบาและรวดเร็ว:
```javascript
function renderDonutChart(percentage) {
  return `
    <div class="donut-chart" style="background: conic-gradient(#0EA5E9 ${percentage}%, #e2e8f0 ${percentage}% 100%)">
      <div class="donut-inner">
        <span class="text-xl font-black">${percentage}%</span>
      </div>
    </div>
  `;
}
```

## 4. แผนการดำเนินงาน (Execution Steps)
1. **Layout Foundation**: เพิ่ม CSS คลาสตาม Spec ลงใน `app.css` (view-pane, pressable, animations)
2. **View Engine**: สร้าง `reports.view.js` พร้อมระบบสลับหน้า Main/Sub
3. **Data Integration**: เชื่อมข้อมูลจาก Backend (`getReports`) และแสดงผลในรูปแบบ Native Chart
4. **Final Polish**: เพิ่ม Skeleton Loading (`skeleton-box`) เพื่อประสบการณ์ที่ไหลลื่น

## 5. การตรวจสอบความปลอดภัย (Security Check)
- ทุก API Call ต้องผ่าน `assertManageStaff_` ในฝั่ง Backend
- หน้า Report ต้องตรวจสอบ Role เสมอ ห้ามแสดงข้อมูลโดยไม่ได้รับสิทธิ์

---
*Generated by Gemini CLI*
